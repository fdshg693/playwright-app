# Step 3: 専用ダッシュボード化（プレースホルダ）

> [02-cost-summary-cli.md](02-cost-summary-cli.md)の続き。詳細は後に記載。

Step1・2でスクリプト実行ベースの履歴保存・複数ファイル/期間指定の集計ができた後、それを人間が都度スクリプトを叩かずに眺められるようにする（Webダッシュボード or 簡易な静的レポート生成など）ためのステップ。着手時に本ファイルへ詳細プランを書く。

現時点で決まっていること:
- Step1・2で作った`{stem}.history/`配下のログ・`cost_report.py`の出力ロジックをそのまま再利用する前提とし、集計ロジックの再実装はしない。
